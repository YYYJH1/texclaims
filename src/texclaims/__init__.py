"""texclaims — deterministic audit of manuscript numbers against artifacts."""

__version__ = "0.1.0"

# texclaims is a command-line tool, not a library: there is no supported
# Python API, and the ledger format is the interface that will be kept stable.
