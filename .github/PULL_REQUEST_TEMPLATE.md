<!-- See CONTRIBUTING.md. Delete any section that does not apply. -->

## What this changes

## What it now refuses to let through

<!-- For a change to gate behaviour: the input that used to pass, and the
     verdict it gets now. Name the test that holds the line. -->

## Checklist

- [ ] `pytest` passes locally
- [ ] Gate behaviour changed? A test in `tests/test_failclosed.py` describes the failure it prevents
- [ ] Console output quoted in a README was pasted from a real run, not edited by hand
- [ ] Ledger schema changed? `docs/ledger.md` says so
